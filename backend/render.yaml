services:
  - type: web
    name: becs-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: DATABASE_NAME
        value: becs_db
      - key: ENVIRONMENT
        value: production
